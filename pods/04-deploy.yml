apiVersion: apps/v1
kind: deployment
metadata:
  name: frontend-deploy
  labels:
    app:
    tier: app1
spec:
  replicas: 1
  selector:
    matchLabels:
      tier: app1
  template:
    metadata:
      labels:
        tier: app1
    spec:
      containers:
      - name: nginx
        image: nginx
